[user]
  email = tannercarbonati@gmail.com
  name = carbonati
[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %
  process = git-lfs filter-process
  required = true
[color]
  diff = auto
  status = auto
  branch = auto
[color "status"]
  changed = yellow
  added = green
  untracked = red
